 <%= form_with(model: @article) do |f| %>
  <p><%= f.label :title %></p>
  <p><%= f.text_field :title %></p>
  <p><%= f.label :content %></p>
  <p><%= f.text_field :content %></p>
  <%= f.submit %>
 <% end %>
